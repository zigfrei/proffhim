export type CallBackFormField = {
  name: string;
  email: string;
  phone: string;
  message: string;
};

export type CertificateCallBackFormField = {
  name: string;
  email: string;
  phone: string;
  company: string;
  message: string;
};